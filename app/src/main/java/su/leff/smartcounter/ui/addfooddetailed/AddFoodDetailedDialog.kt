package su.leff.smartcounter.ui.addfooddetailed

import android.app.AlertDialog
import android.content.Context

class AddFoodDetailedDialog(context: Context) : AlertDialog(context){
    override fun show() {
        setTitle("Tas")

        super.show()
    }
}